<p><%= link_to("Dodaj post", new_post_path) %></p>

<% @posts.each do |post| %>

  <h2><%= post[:title] %></h2>
  <span><%= post[:date] %></span>
  <p><%= post[:content] %></p>
  <p><%= link_to("Edytuj", edit_post_path(post)) %></p>
  <p><%= link_to("Usuń", post_path(post), {method: :delete}) %></p>
<% end %>
